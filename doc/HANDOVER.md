# [2026-05-01] Handover Notes | 物理構造変更の実行依頼 (To Antigravity)

## 【1. このチャットの目的】
- **物理的なディレクトリ移動の完遂**: `html/` 内のファイルをルートおよび `tools/` フォルダへ移動し、不整合となるパスを全ファイルで一括修正する。
- **URL構造の正規化**: ポートフォリオをトップ (`/`)、ツール一覧を `/tools/` で表示可能にする。

## 【2. 新しいチャットに切り替える理由】
- **権限の委譲**: ファイルの移動（物理的な move 操作）を伴う大規模なリファクタリングを、より強力な操作権限を持つ Antigravity (Gemini 3 Flash Preview) に任せるため。また、サイト管理を`HEXO`に切り替える準備を進めるため。`doc\HEXO_MIGRATION_ROADMAP.md`に記載されたフェーズ1からフェーズ5の内容を実行するための前段階として、現行のファイル構成を新しいルールに従って整理する必要があります。

## 【3. 背景・前提条件】
- **新ルール (`ARCHITECTURE.md`)**:
    - ルート (`/`): `index.html` (ポートフォリオ) を配置。
    - `tools/`: 全てのツール HTML を配置。`tools-index.html` は `tools/index.html` へ改名。
    - パス解決: ルート配置ページは `public/`, `styles/` を直下参照。`tools/` 内は `../` 経由で参照。

## 【4. ここまでの経緯】
1. 全 18 ツールページのコンテンツ拡充（1,000文字以上の解説記事化）が完了。
2. サイドバーの共通化、アコーディオン化、リクエストフォームの構築が完了。
3. 内部推論 AI ロジック（背景透過、超解像、消しゴムマジック）の正常動作を確認。
4. `doc\ARCHITECTURE.md` を日本語で更新し、新ディレクトリ構造を定義済み。

## 【5. 決定事項】
- **`html/` フォルダの廃止**: すべてのファイルをルートまたは `tools/` へ逃がし、空になったフォルダは削除する。
- **パスの整合性保証**: `../public/` や `../styles/` を、新配置に合わせて一括置換する。

## 【7. 次のチャットで最初に依頼すべき内容】
```markdown
`doc\ARCHITECTURE.md` および `doc\HANDOVER.md` を読み込み、構造変更を実行してください。
1. `html/index.html` をルートへ移動し、パスを修正。
2. `html/` 内のその他のファイルを `tools/` へ移動（`tools-index.html` は `tools/index.html` に改名）。
3. 移動した各ファイルの資産パス（public, styles）を、移動後の階層に合わせて正確に修正。
4. 空になった `html/` フォルダを削除してください。
```

## 【8. 引き継ぎ本文】
お疲れ様です。VOLCANEプロジェクトは、静的サイトとしての堅牢性を維持しつつ、モダンなWebアプリケーションとしてのUIを確立しました。

### 現在のファイル構成と状態
- **`public/sidebar.js`**: 全ページの左メニューを司る心臓部です。
- **`styles/tool-details.css`**: 全ツールの共通外観（センター配置、アイコン、ワークスペース）を定義しています。
- **`html/*.html`**: 各ツール個別ページ。すべてサイドバープレースホルダー (`<aside id="sidebar-placeholder">`) への移行が完了しています。

### 直近の重要成果
1. **反映エラーの根絶**: HTML構造の不一致によるパッチ適用失敗を、物理アンカー（一意のコメント行や構造タグ）の徹底により解消しました。
2. **消しゴムマジックAIの復活**: `object-remover.html` は現在、実際に画像を修復できる AI Inpainting 機能を搭載しています。
3. **ナレッジの永続化**: `LLM_Wiki_Link` 内に、将来のプロジェクトで役立つ「再利用可能なテクニック」がまとめられています。

### 次のアクション
まずは、`summarizer.html` に本物のAI推論ロジックを吹き込んでください。その後、`image-upscaler.html` の高度化、または `sidebar.js` への「New!」バッジ機能の追加などが控えています。

---
*Updated: 2026-05-01 | Assistant: Gemini 3 Flash Preview*
